# 2026-06-17 — Session Log

## Session — Expand D1 (architecture) + framework placement review

**Trigger:** Operator: (1) expand D1; (2) keep `regen-toolkit-os` as a parallel branch (not promoted to main); (3) review the framework — where/how it's placed and operates — before continuing to the next plans.

### Decisions
- **`regen-toolkit-os` stays a parallel branch.** Live site keeps deploying from `main` (site team's lane); `regen-toolkit-os` is the consolidated org-os + framework lane; pull from `main` periodically. Updated HEARTBEAT + memory references that implied promotion.

### D1 — expanded into a decision memo (`docs/plans/architecture-lifecycle-vs-layers.md`)
- Grounded in `docs/MASTER.md` §4 (Core movement, Minimum Operating Kernel, Layer stack, Cross-layer mapping) + §10–20 (Knowledge Lifecycle). **The master doc already does both:** lifecycle = how knowledge *moves* (verbs); layers = what it's *made of* (nouns).
- Produced the full **lifecycle↔layer↔kernel-object mapping table** (Capture→L3, Understand→L2, Relate→L1, Compose→L5/L4, Specify→L6/L7, Implement→L8, Learn→L8, Evolve→L9, Steward→cross-cut, Interoperate→L10). Honest seams noted (Ontology spans all; "Steward" has no layer → cross-cutting; Specify joins two layers).
- **Recommendation (C):** Knowledge Lifecycle = human-facing spine; 10 Layers = structural/data model; Ontology + Stewardship/CSIS cross-cutting. Matches the master doc's Structure Options rec ("Small Core, Large Appendices, Lifecycle spine"). De-risks P1 (data model NOT rebuilt; layer docs/canvases kept as the structure reference). 5 open questions for the group.

### Framework review (`framework/PLACEMENT.md`)
- **Topology grounded** (federation.yaml + vault): regen-toolkit is *already* an org-os instance (overlay sync from `org-os-template`); `regen-coordination-os` IS the org-os framework; refi-dao-os/refi-bcn-os are sibling instances.
- **Core reframe:** the "Regen Knowledge Commons Toolkit framework" = a **knowledge-commons *profile* of org-os** (a specialization layer between org-os substrate and domain instances). This dissolves most of the framework-vs-instance confusion and reuses the existing template→instance→overlay pattern.
- **Placement recommendation:** hybrid/phased — keep the spec in `framework/` now; push generic bits upstream to org-os-template as they stabilize; **let P9 (refi-dao-os) force the profile extraction** to its own template repo (no premature abstraction). Don't restructure yet — validate the boundary via the P3 prototype first. "Recognize the boundary before relocating it."
- **Operation model:** the lifecycle loop on the org-os substrate (`/initialize` → capture via skills → understand/relate/compose/specify → CSIS review → implement/learn/evolve → interoperate/generate site → `/close` → federate via RegenOS). Framework = what you adopt; RegenOS = how adoption federates you in (two halves).

### Framework model REVISED (operator direction) — PLACEMENT v2
- Operator reframed the framework as a **standalone, org-os-AGNOSTIC package** (`packages/toolkit-framework`), usable in any context with or without org-os — **inverting** my earlier "profile-of-org-os" model. org-os integration becomes a separate, **replaceable** module (`org-os-kms` = "org-os Knowledge Management System": full setup/manage toolkits + commons).
- Goals for the framework package: encompass **all** of the master-doc framework; be a portable **artifact to easily adopt** it; **ship agentic skills directly** for agnostic adoption/operation.
- **PLACEMENT.md rewritten to v2:** 3 modular layers (toolkit-framework core ← org-os-kms ← instance); package layout (`architecture/ schemas/ process/ site/ skills/ templates/` + CLI); the concrete current→package **refactor map** (§6); package-first, mirror-to-repo-when-stable.
- **P1 + README aligned** to the package model. Packages follow the repo's `@org-os/*` TS convention (`src/`→`dist/`, CLI bin) — but the framework scope must be **neutral** (`@regen-commons/...`), NOT `@org-os/`.

### Status / next
- **Awaiting operator/group confirmation** on: the modular model, the package name/scope, `org-os-kms` home (here vs `regen-coordination-os`), spine-first vs full-completeness for v1.
- Next concrete step once confirmed: **scaffold `packages/toolkit-framework`** (skeleton + first agentic skill) — but **D1 gates `architecture/`**.
- Did NOT scaffold the package or move files yet (still in design review).
