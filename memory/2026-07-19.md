# Memory — 2026-07-19

**Focus:** A multi-day build+integration arc — the framework became a running machine, the toolkit ate its own cooking, Matty's July handoff was integrated (and shown to be the same system), the framework evolved to close the crosswalk gaps, and the repo-migration asks went to Heenal. Next: build the full live **dev instance** in Luiz's personal fork so review can start without waiting on the migration.

_(Session spanned 2026-07-14 → 19; logged 07-19.)_

## What shipped

- **Framework Validation Pass (V0–V4) — done.** 4 kernel fixes (source-system enum · `held` state · `track.outcome`→array · **B5 silent-overwrite guard**; 100→110 tests). Self-ingestion: the toolkit's **119 live articles → 722 typed objects** (`data/kb/`, all `raw`). Artifacts: `/self-ingestion/` page, canvas, diff report, an 8-Forms-of-Capital `update-proposal` (draft). Consolidated report published. Plan: `docs/plans/framework-validation-pass.md`.
- **Handoff Integration (Matty's July package) — T1/T2/T3a/T5 + T4-core done.** Core finding: **his Database_Spec = the framework machine** (zones = maturity ladder; his 87 dup-flags ≈ the B5 guard; DoD #1 = the idempotency we demonstrated). Built the **§40 crosswalk** + **Definition-of-Done conformance** (7✅/8🟡) — the returns his Guide asks for. **T3a:** ran a slice of his Canonical_DB through the machine (127 rows → 146 objects in `kb-handoff/`; B5 caught his 4 hand-flagged dup pairs automatically) + `/handoff` page. **T5:** Matty agent-setup pack + call brief. Plan: `docs/plans/handoff-integration.md` · orientation: `docs/HANDOFF-CHANGES-2026-07.md`.
- **T4 framework evolution (core) — done.** Closed the crosswalk gaps: **`relationship-record`** (first-class sourced-assertion edge — his Core Decision #3), **`person` + `organization`** entities, **currentness/confidence/maintenance** status axes, enum gaps. **110 → 125 tests**, kernel consistent. Remainder logged in the master plan (PUB-extends-frontmatter migration, case-study schema, CLI ergonomics, relationship-array migration).
- **Comms:** Heenal repo-migration asks **sent** (`docs/comms/drafts/2026-07-16-heenal-asks.md`) — 3 blocks: migrate repo → RC org · reconnect prod deploy · open a Netlify **dev build** on `regen-toolkit-os`. Group + Matty share pack drafted (`2026-07-15-share-pack.md`, agent-voice).
- **Housekeeping:** HEARTBEAT tasks made identifiable (refi-bcn-os `task-YYMMDD-owner-slug` convention, 30 tasks); handoff source gitignored; all working-tree state committed.

## Current state

- Branch `regen-toolkit-os` = dev; `main` = prod (auto-deploys to the live site on merge).
- Live (fork Pages): `/self-ingestion` (722) · `/handoff` (146) · `/convergence` · `/framework` · `/regen-toolkit-os`. These are **summary** pages (counts/diffs) — the reprocessed content is **not yet rendered as browsable content pages**.
- Framework: **125 tests green**. KB: `data/kb/` 722 · `kb-handoff/` 146 — all `raw`, review-gated.
- ~7 commits ahead of `fork`; ~11 ahead of `origin` (incl. parallel-session work — push is operator's call).

## Next direction (this decision, 07-19)

Build the **full live dev instance in Luiz's personal fork** — the full Astro build rendering the **reprocessed content following the framework** — so review can start now, independent of Heenal's migration. Matches the 2026-07-16 meeting plan (staging site side-by-side vs the current build → checklist + named-reviewer sign-off → PR to `main`). Fork prompt: `docs/plans/SESSION-HANDOFF-2026-07-19.md`.

## Carry-forward (parallel sessions)

1. **Compare/validate** the new (framework-reprocessed) instance vs the old (119-article) build — graph-view relationship shifts, coverage, what changed.
2. **Consolidate plans + finish T3b** — the full Canonical_DB ingestion (2,689 rows) via the machine (sub-plan written; now uses the T4 schemas — relationships + person/org have homes).
3. **Preserve Heenal's GitHub PM system** — issues, logs, tasks, GitHub Projects on his repo must survive / be carried over in the org migration (don't lose his mapped work).
4. Standing: human review pass (868 raw objects) · privacy/name-flagging gate before public · framework-validation-pass 7-item feedback harvest · T4 remainder.
