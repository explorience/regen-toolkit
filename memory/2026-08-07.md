# Memory — 2026-08-07

## Session — process the 2026-08-06 engineering sync + reconcile the Artizen strategy

**Focus:** Full processing of the **2026-08-06 Regen Toolkit Engineering Sync** (Google Meet + Gemini notes + complete 1h27m transcript) into the org-os overlay, then propagating what it changed into the hub-level Artizen strategy note.

### Meeting: Regen Toolkit Engineering Sync (2026-08-06)

Attendees: Afo, Matty, Luiz, Rather, Regis Chapman (Durgadas). The first sync after the 07-16 biweekly, and **the one where process infrastructure landed**.

**Key decisions**

- **Linear adopted** as the project-management tool (Afo leads) — initiatives → projects → issues; **two-week cycles starting the day after the biweekly**; exponential estimates (nothing at 16); free plan = everyone admin; AI (Claude/Codex/GPT/Cursor) + GitHub integrations are the reason it was chosen; templates, labels, documents, recurring issues, Customers. Seeded on two tracks — Afo from the call notes + a GitHub issue import, Luiz from the `regen-toolkit-os` processed notes.
- **Cloudflare Workers adopted for hosting** (Rather, needs org access) — branch previews + built-in CI/CD. Chosen for the **contribution pathway**, not the cost saving.
- **Dedicated coordination group** (Afo to create) — stops the split across the top group and the Regen Coordination council group.
- **Repo merge sliced into four ordered phases:** framework + schemas w/ tests → deterministic generators + validation → public-safe aggregate data + selected Astro surfaces → reviewed knowledge objects *after the promotion gate*. Internal org-os coordination material stays out of the production projection.
- **Afo's amendment, which is the real reordering:** stabilise **dev and prod as mirrors** first so outside contribution can start; heavier org-os work stays on its own branch until then. *"The biggest thing I saw is that right now it's hard for others to contribute."*
- **Schema convergence resolved** — converge on **integration points** and pin versions (strict Zod/TypeScript, validated on send *and* receive), but **a distinct schema per object class**, not one flattened model. Regis's law-vs-oracle-vs-time-standard objection was accepted. **Convergence is on the seams, not the shapes.** Standing constraint on `data/ontology/` work now.
- **The ingestion layer is the near-term gate** — Matty's four-stage frame (functionalise the engine → improve existing knowledge → feed the engine + human review → tracks/deployment), with Afo adding that source-validation must filter before human review *"so we don't overload our human small brains."* **This is the stated reason the org-os work waits.**
- **Standards ship as code, not documents** — Rather's **probabilistic-interpretation problem**: an agent can't attend to a whole standards doc, so *"give the document to a thousand agents, get a thousand interpretations."* Reference implementation + skills + prompts instead. Rather + Regis collaborating.
- **Organise around roles, boundaries and Customers** (Afo) — who this is built for outside the close circle.
- **Modular, engine-agnostic agent components** over one self-modifying harness; **prefer hosted** (Harmonica, Bonfire cloud) over self-hosting.

**Identity resolution — `Regis Chapman` = `Durgadas`.** The 07-16 note listed them as two separate participants. Evidence: Regis owns CRAFT / ore-struck-craft / the integrity suite / the standards corpus / the new time standard; Afo calls him "Das", "Dodos", "Derodos" mid-thread; his preferred email domain is `deradosmack.com`. Corrected in the new note, the registry and the integration report; **the 07-16 note and `data/` participant lists still carry the split and need a sweep.**

**Two 07-16 items superseded.** The **Netlify dev build** ask to Hina → **Cloudflare branch previews**. **GitHub Projects** → **Linear**. Live remnant: check Hina's mapped issues survive the migration before Afo's import runs.

**Post-close tail (~15 min, worth keeping):** Harmonica is becoming a **deliberative runtime** — deterministic/programmatic rather than LLM-synthesised, arbitrary input, **CLI + MCP**, Pol.is-class; Rather is paying for it. **Bonfire** is knowledge-graph memory that requires a **rigid ontology loaded early** or you get *"slot mess"* and eventual-consistency agent behaviour — import the schema as documents first, verify against a small single-player memory system, then scale; self-hosting is container-heavy, pay for the cloud. **Local Apple Silicon inference**: fast-small for recall (blocks the turn), ultra-small for async ingestion, larger for reflection (2–3k-token structured prompts make quantised 100B+ viable). Afo has an idle Mac Studio for exactly this.

**Labor Fund** (recorded as a cautionary parallel): NanoClaw-based monolith on one person's Mac Mini, one burning-out developer, no visible codebase for contributors — *"no opportunity for contributors to say: I have capacity."* The same structural failure the four slices, Cloudflare previews and Linear are meant to prevent here.

### Artizen — what the meeting changed

Processed the first 15 minutes into the hub strategy note `260807 Artizen Season 7 - Consolidated Strategy.md` (§0.1 added; §1, §3, §4, §5, §6, §7, §9, §10 revised).

- **§5's premise was wrong on one point.** The doc said the bulk-boost question *"has never been asked."* Afo answered it live on 08-06: *"you just got to click like a mad man… it's one boost at a time, but eventually it does more than one at a time."* No bulk endpoint, but **the click accelerates**. R6 downgraded from binding constraint to friction; the Venus half of the ask still stands.
- **New mechanics:** boosts buy **fund cash-prize ranking** and **raise matching availability** — they do **not** fund projects (Green Goods sits at **27k matching because it's in multiple funds** — direct evidence for Q2's per-fund reading). **Project boosts are near-worthless** (last-minute entrants blow you out). **Spend late against the leaderboard**, never front-loaded. Replenishment ≈ **20k/week showcase** (~80k/month); Afo's floor is ~100k/month.
- **This week's allocation was decided:** weight behind the **BREAD fund** — Luiz ~100k, Afo 50–100k of his ~400k. Matty's leaderboard-presence boost on the RC project was talked down to a single probe boost.
- **~$164 (+10% ≈ $180–182) executed live from the safe**, plus a Solidarity Fund transaction and a send to Matt's Artizen wallet. Far below the planned $1,000/$250 — deployment happened, at a tenth the size. *(Amounts reconstructed from noisy audio — verify against the safe.)*
- **Balance discrepancy:** Afo cited Luiz at 600k; Luiz said "I have like 120k." Unresolved.
- **R7 (Vercel preview URL as the public surface) now has a fix path** — Cloudflare, owned by Rather, gated only on org access. Downgraded 🔴 → 🟡.
- **Rather's Artizen ownership:** the doc's *"Rather is out of the ownership map entirely"* is right for the **Artizen entity** and wrong as a general statement — he took **hosting, schema convergence and the Harmonica purchase** on 08-06. Scoped the claim.

### Actions taken

- [x] **Meeting note** — `packages/operations/meetings/260806 Regen Toolkit Engineering Sync.md` (full structured note: decisions, action items by owner, Artizen mechanics table, discussion summary, transcript-quality + identity notes).
- [x] **`data/meetings.yaml`** — `mtg-20260806-regen-toolkit-engineering-sync` prepended (11 decisions, 6 action-item groups, 12 tags).
- [x] **`HEARTBEAT.md`** — new `2026-08-06 engineering sync` section (21 tasks, `task-260806-*`); marked the two superseded 07-16 items in place.
- [x] **Integration report** — `docs/reports/2026-08-06-engineering-sync-integration.md` (shareable; draft-and-present, not yet posted).
- [x] **Source note** — frontmatter fixed (date, projects, categories) + processed pointer added.
- [x] **Artizen strategy note** — reconciled against the meeting (see above).

### Next

- [ ] **Give Rather GitHub org access** — one permission grant, unblocks the whole Cloudflare dev-review surface. Do it first.
- [ ] **Post the integration report** to the toolkit group (operator approval — it's draft-and-present).
- [ ] **Sweep the Regis/Durgadas split** out of the 07-16 note and any `data/` participant lists.
- [ ] **Seed Linear** from the `regen-toolkit-os` branch before Afo's structure hardens, so the repo/engine issues land with real detail.
- [ ] **Verify the boost balance** (600k vs 120k) and the 08-06 safe transactions.
