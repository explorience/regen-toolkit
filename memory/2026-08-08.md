# Memory — 2026-08-08

## Session — Cloudflare Option B stood up: both instances live

**Focus:** Execute the 08-06 Cloudflare adoption ahead of the Rather handover — Option B (two Workers projects), both instances deployed and serving.

### What's live

| Instance | Worker | URL | Source | Build |
|---|---|---|---|---|
| **Prod** | `regen-toolkit` | https://regen-toolkit.luizfernandolfsg.workers.dev | `origin/main` @ `4bf9cecf` | 124 pages / 290 files |
| **Dev** | `regen-toolkit-dev` | https://regen-toolkit-dev.luizfernandolfsg.workers.dev | `regen-toolkit-os` working tree | 3,931 pages / 4,099 files / 47 MB |

Smoke-tested: `/`, `/validation/`, `/kb/` all 200 on dev; prod root 200.

### Decisions

- **Option B confirmed (operator):** two separate Workers projects, not one project with branch previews — dev builds surfaces (`/validation`, `/kb`, handoff pages) that should never auto-appear on prod.
- **Static-assets-only Workers** (no server code, no `@astrojs/cloudflare` adapter — both branches are static output with `base: '/'`; the GitHub-Pages base only activates under `GITHUB_PAGES=true`).
- **No config file committed to `main`** — prod deployed via CLI flags (`--name regen-toolkit --assets ./dist`) from a disposable git worktree of `origin/main` (vault-safe; worktree removed after). Dev's config lives in `wrangler.jsonc` on `regen-toolkit-os` (**uncommitted** — commit with the session's work).
- **Deployed under Luiz's personal Cloudflare account** (`luizfernandolfsg@gmail.com`, OAuth via wrangler). This is a bootstrap, not the end state — handover to Rather / a team account is the open item.

### Constraints verified

- Cloudflare's **20,000-files-per-deployment cap**: dev = 4,099 files — comfortable headroom, but the KB corpus grows; recheck if kb-handoff (3,058 objects) gets rendered as pages.
- No file over the 25 MiB per-file limit.
- `origin` is already `github.com/regen-coordination/regen-toolkit` — the org repo exists, so the Cloudflare **git-connection** (build-on-push) can target it directly; Rather still needs org access.

### Follow-up (same day) — Cloudflare agent setup + deploy tooling

- **Cloudflare agent environment installed** per `developers.cloudflare.com/agent-setup/prompt.md`: `cloudflare@cloudflare` plugin (13 skills: wrangler, workers-best-practices, durable-objects, agents-sdk, …) + 5 MCP servers (api/docs/bindings/builds/observability). MCP servers need a full Claude Code restart to load; skills reload alone didn't surface them.
- **Deploy scripts built + verified end-to-end**: `npm run deploy:cf` (dev) / `npm run deploy:cf:prod` (prod via disposable origin/main worktree) — `scripts/deploy-cloudflare.mjs`, with pre-upload checks against the 20k-file / 25 MiB limits. Both ran green; asset dedup makes re-deploys ~15s.
- **Handover doc written**: `docs/onboarding/cloudflare-hosting.md` — state, manual deploys, the 5-min Workers Builds dashboard checklist, Rather handover items, watch-items.
- **Finding:** the Workers Builds git-connection (build-on-push + PR previews) requires installing Cloudflare's GitHub App on the `regen-coordination` org — a browser step needing GitHub org admin; no CLI/API/MCP path around it.

### Not done yet (the actual handover)

- [ ] **Build-on-push** — the dashboard checklist in `docs/onboarding/cloudflare-hosting.md` (needs GitHub org admin + the Cloudflare account). Until then: `npm run deploy:cf` / `deploy:cf:prod`.
- [ ] **Rather handover** — either add him to the Cloudflare account or transfer the two Workers to a team account; plus GitHub org access (still the ★ item).
- [ ] **Custom domain on prod** — retires the Vercel-preview-URL credibility exposure (Artizen `R7`); trivial once a domain is chosen since Cloudflare is the DNS layer.
- [ ] **`site:` URLs** in both `astro.config.mjs` still point at Vercel/GitHub-Pages domains — update canonicals/sitemaps when the real domain lands.
