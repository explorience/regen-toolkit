# Memory — 2026-07-22

## Session — T3b full Canonical_DB ingestion + plan-surface consolidation

**Focus:** Executed **Prompt 3** from `SESSION-HANDOFF-2026-07-19.md` — consolidate the plan surface, then run the **full T3b Canonical_DB ingestion** through the framework machine. Committed + pushed (`6ff1406a` → `origin/regen-toolkit-os`).

_(Session spanned 2026-07-21 → 22.)_

### Key Decisions

- **Hybrid deterministic ETL over an LLM-runner swarm.** The sub-plan said "dispatch subagent runners to hand-author 2,689 candidates." Reading the machine, the framework already owns a deterministic crosswalk-driven CSV→object ETL (`lift.mjs`), and Matty's normalization config (object-type-crosswalk, predicate-map, controlled-vocab) is literally a set of lookup tables meant to be applied mechanically. Chose a **deterministic mapper** (born-rules + crosswalk lookups + verbatim text) validated by the framework's **own accept gate**, with judgment reserved for flagged/ambiguous rows. **Why:** it satisfies Matty's DoD #1 (idempotent re-run — an LLM swarm can't), gives exact row→object accounting for DoD #15, and *is* the framework's ETL design, not a deviation. Operator approved.
- **Fresh rebuild of `kb-handoff/`** (cleared the 146-object T3a slice; git-recoverable) for clean 1:1 row→object accounting — no double-counting the slice.
- **Isolated work-order dir** `.workorders-handoff-full/` (gitignored, regenerable) — avoids hash collisions with the T3a slice + the 722-object self-ingestion WOs; keeps the migration self-contained.
- **Boundaries, principled not blanket:** every named `person` → `never-publish-without-consent`; genuinely sensitive content (tight regex: surveillance/indigenous/at-risk/activists/…) → `requires-domain-review`. Tightened from an over-firing first pass.
- **Matty's 248 `review_needed` predicate sentinels** kept the original verb as the edge (so relationships stay meaningful) + a review flag — not written literally as the predicate.

### Actions Taken

- [x] **Part A — plan surface consolidated** to one active spine: `dev-instance-build` (#1) + `handoff-integration` T3b (#2); `framework-validation-pass` complete; `CONVERGENCE-PIPELINE` the strategic map (fixed its Plans-table drift — dead links → `archive/`, true statuses). Most was already staged by a prior session; I de-drifted the last inconsistency.
- [x] **Phase 0** — `export-handoff-full.py`: 6 families (2,689 rows) + normalization config exported from Canonical_DB.xlsx.
- [x] **Phase 1** — `map-handoff-full.mjs`: deterministic mapper → 2,957 candidates; all passed the accept gate (schema + invariants + born-rules, atomic).
- [x] **Phase 2** — stored → `kb-handoff/` = 2,957; B5-vs-Matty-flags reconciliation (42 collision sets = his exact dup-flags).
- [x] **Phase 3** — `map-discovery-priority.mjs`: Discovery_Pool Priority Working Set (114) → 98 promoted (11 deferred already-canonical, 5 excluded workbook-meta). Final `kb-handoff/` = **3,058 objects**.
- [x] **Deliverables** — migration manifest (`docs/reports/2026-07-21-canonical-migration-manifest.md`, DoD #15), `data/kb/_handoff-full-manifest.yaml`, `/handoff` page numbers updated (site builds, 3,930 pages), 125 tests green.
- [x] **Committed + pushed** (`6ff1406a`), scoped; `.workorders-handoff-full/` gitignored.

### Next

- [ ] **Human review pass** — 2,787 raw objects are now the corpus for the review-promote gate + named-reviewer checklist (Prompt 2).
- [ ] **`dev-instance-build`** (Prompt 1) — render this corpus as browsable content; also owns the `kb-graph/` snapshot regeneration (the build left partial regen uncommitted this session).
- [ ] **Discovery_Pool remainder** (4,837 rows, Zone A) — later bounded promotion batches.
- [ ] **Reusable for other instances:** the deterministic-ETL approach (config-as-lookup-tables + accept-gate validation) is the pattern for ReFi DAO / ReFi BCN corpus ingestion.

## Session — 10:55 · Prompt 2 — reprocessing review gate (analysis + process + live dashboard)

**Focus:** Executed **Prompt 2** from `SESSION-HANDOFF-2026-07-19.md` — the review gate before any PR to `main`, comparing the framework-reprocessed KB (`data/kb`, 722 typed objects) vs the live 119-article build. Committed + pushed to **fork** (`ac8a10e2`), deployed live, verified. The user also asked me to add the missing build surface for reviewing → shipped a `/validation` dashboard.

### Key Decisions

- **Scope = the 119→722 self-ingestion corpus**, not Matty's `kb-handoff/` (separate Canonical_DB source). The coverage map + graph diff are grounded in provenance links `data/kb` objects carry back to `src/content/docs/*.md`. The named-reviewer checklist/protocol are corpus-agnostic and extend to the (now 3,058-object) handoff corpus.
- **"Thinned" = low object-yield vs source length** — a reviewer-attention flag, not an automatic defect (glossaries/process pages legitimately yield few objects).
- **Graph diff is resolution + coverage, not a single ratio.** Old build: 666 edges but 610 from 12 coarse `systems:` tags + a curated core on only 41/119 articles. New: 161 pairs anchored in 191 concepts/183 resources across all 119, 139 emergent. Honest counter-weight: only **60.4%** of typed refs resolve exactly (85.4% with an alias layer) — the graph is **not build-ready** until refs are normalized.
- **PROMPT 2 does have a build surface** (user's catch): the *content* staging site is Prompt 1's job, but the review-*gate dashboard* is Prompt 2's — a `/validation` page following the `/self-ingestion` pattern, reading a build-input JSON snapshot the analysis script emits.
- **Vault-safe scoped commit:** committed only my 11 files; deliberately left the 2,196 `kb-graph/` files + modified HEARTBEAT/plans to the parallel sessions. Pushed to **fork** only — `origin`/`main` untouched.

### Actions Taken

- [x] **Coverage map** — `scripts/validation/reprocessing-coverage.mjs` (read-only, reproducible) → `data/validation/coverage-map.{yaml,csv}`. 119→722, **0 dropped, 0 orphans**, mean 6/article, 11 thinned pages flagged.
- [x] **Graph-view diff** → `data/validation/graph-diff.yaml`. 139 emergent connections (e.g. 3 payments articles linked via *The Giving Block*).
- [x] **Reference-integrity check** (motivated by the spot-check) — 392 exact / 162 naming-drift / 95 truly-dangling of 649 refs; 60.4% → 85.4% w/ alias. Quantified a systemic finding.
- [x] **Faithfulness protocol** (`docs/reviews/faithfulness-spot-check-protocol.md`) + **worked round via subagent**: 11/12 faithful, 0 unfaithful; 2 minor (Greenpill=`blog` type misfit; dangling refs). Protocol calibrated (stratify by `transformation`; automate ref-integrity).
- [x] **Named-reviewer checklist** + 119-row tracking sheet (3 priority tiers 43/67/9) + **"shareable initial version" DoD** (both meetings asked for it).
- [x] **Validation report** `docs/reports/2026-07-19-reprocessing-validation.md` (ties D1–D4).
- [x] **`/validation` dashboard** (`src/pages/validation.astro`) — built, deployed to fork Pages, verified live at `luizfernandosg.github.io/regen-toolkit/validation/` (200, real data).
- [x] **Site build verified** — 1015 pages clean (was 131; Prompt 1's `/kb/` content pages have since landed). Build surface I touched: none in `src/` beyond the new page.
- [x] **BACKLOG** — routed 4 framework-feedback items (alias layer [high-risk], claim+uncertainty pairing, type-slot sanity, gate-ready).

### Next

- [ ] **Reviewer lead + page assignments** — deferred to the **next call** (decision 2026-07-21). Unblocks the ~2–4 wk window.
- [ ] **Reference-normalization / alias layer** — `high-risk`, **blocks Prompt 1's graph view** — flag to whoever owns the dev instance.
- [ ] **`claim` + `uncertainty` render-together rule** + **type-slot sanity check** → `toolkit-framework`.
- [ ] The gate now also covers Prompt 3's expanded `kb-handoff/` (3,058 objects) — the checklist/protocol apply; coverage/graph analysis there is a future run.
- [ ] HEARTBEAT "review gate ready" note left for the owning session (it had uncommitted parallel edits — not clobbered).
