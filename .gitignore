# Dependencies
node_modules/
.pnp
.pnp.js

# External agent skills (installed via feynman install-skills --repo)
.agents/skills/feynman/

# Build / output
dist/
.astro/
build
out
*.log

# Vercel
.vercel

# Local env files
.env
.env*.local

# Superpowers plugin ephemeral plans
docs/superpowers/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~
.claude/worktrees/

# OS / sync
.DS_Store
*.sync-conflict-*
*.pem

# Toolkit working dir (scratch)
working/

# Matty handoff source (read-only, ~10MB binary; canonical copy on Google Drive)
docs/RKC_Handoff_July_2026_FINAL_VERIFIED/

# Transient tooling / scratch (keep a fresh clone clean)
# .superpowers/ = brainstorming visual-companion server files
# .tmp/ = handoff docx text extractions + scratch; docs/temp/ = scratch drafts
.superpowers/
.tmp/
docs/temp/

# T3b work-order pipeline state — regenerable scratch (the corpus lives in kb-handoff/;
# the scripts in scripts/validation/ + the manifest reproduce it). Mirrors kb-handoff/.
.workorders-handoff-full/
