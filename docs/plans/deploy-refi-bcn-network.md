# P10 — Deploy → ReFi BCN + Network Availability

> **Status:** queued · **Wave:** 4 · **Owner:** Luiz · **Target instances:** `../refi-bcn-os`, then the wider network
> **Skills:** `superpowers:subagent-driven-development`; `superpowers:using-git-worktrees`
> Part of the [convergence pipeline](CONVERGENCE-PIPELINE.md). **Depends on:** P9 (proven instantiation procedure).

## Goal
Instantiate the framework for **ReFi BCN** (`../refi-bcn-os`, the bioregional/local-node instance) using the procedure proven in P9, then make the framework **available to the network(s)/community** for self-qualifying adoption (the ~5–7 high-trust orgs target).

## Context
- ReFi BCN is the more mature org-os instance (folder-per-project, pipelines, roles, calendar) — a strong test of the framework against a richer instance.
- Bioregional/local-node angle connects to **Andrea's bioregional knowledge** (bioregioning.org / Recover) + contribution governance (P8).
- "Available to the network" = the **self-qualifying adoption** filter (RegenOS) goes live: any high-trust org adopts the framework + runs the process → federates in. Not open chapter calls — existing high-trust orgs (2026-06-15 scale model).

## Phases
1. **Instantiate for ReFi BCN** (P9 procedure) — local-node/bioregional content; reconcile with refi-bcn-os's existing structure.
2. **Onboard Andrea's bioregional knowledge** (P8 contribution governance) as an external-contributor test.
3. **Package the framework for adoption** — a clean "how to adopt" path (the one-pager + RegenOS docs from P6); the instantiation procedure as a repeatable, documented flow.
4. **Open to the network** — invite the high-trust orgs (with Matty; the 2026-06-15 "press this forward" pair); each adopting org self-qualifies for federation.

## Deliverables
- A running ReFi BCN instance on the framework.
- A documented, repeatable adoption path for any org.
- Network availability (RegenOS federation open to qualifying adopters).

## Definition of done
- ≥2 external instances (ReFi DAO + ReFi BCN) run the framework; the adoption path is documented + repeatable; the network can self-qualify in. **Pipeline complete.**

## Interrelations
- Completes the pipeline. Validates P1 at scale, activates RegenOS (P6) + governance (P8) network-wide, and realizes the 2026-06-15 vision: "any org could adopt the framework... and have it naturally federate into the commons."
